<!DOCTYPE html>
<html>
  <head>
    <%  @welcomes = Welcome.all  %>
    <% @welcomes.take(1).each do |w| %>
    <title><%=w.title %> || <%= w.desc %> </title>
    <meta name="description" content="<%= w.desc %>">
    <meta name="keywords" content="<%= w.section %>">

    <% end %>
        <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= favicon_link_tag asset_path('favicon.svg') %>

    <%= yield :head %>

    <link rel="manifest" href="/manifest.json">
    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
    <link rel="stylesheet" href="https://atugatran.github.io/FontAwesome6Pro/css/all.min.css" >
    <!--Start of Tawk.to Script-->
    <script type="text/javascript">
    var Tawk_API=Tawk_API||{}, Tawk_LoadStart=new Date();
    (function(){
    var s1=document.createElement("script"),s0=document.getElementsByTagName("script")[0];
    s1.async=true;
    s1.src='https://embed.tawk.to/670d9d022480f5b4f58d625d/1ia6iqkpn';
    s1.charset='UTF-8';
    s1.setAttribute('crossorigin','*');
    s0.parentNode.insertBefore(s1,s0);
    })();
    </script>
    <!--End of Tawk.to Script-->
    <script type="text/javascript">
    function googleTranslateElementInit() {
      new google.translate.TranslateElement({pageLanguage: 'en', layout: google.translate.TranslateElement.InlineLayout.HORIZONTAL}, 'google_translate_element');
    }
    </script>
    <script type="text/javascript" src="//translate.google.com/translate_a/element.js?cb=googleTranslateElementInit"></script>

  </head>

  <body>
    <% @abouts = About.all.order('created_at ASC') %>
    <% @services = Service.all.order('created_at ASC') %>
    <% @welcomes = Welcome.all %>
    <% @headers = Header.all %>
    <% @blogs = Blog.all %>
    <% @features = Feature.all %>

    <% @faqs = Faq.all.order('created_at ASC') %>

    <div class="wrapper ">
      <div class="preloader"></div>

      <% if Account.where(user_id: current_user.id).exists?%>

      <%= render "shared/menu" %>
      <div class="dashboard_content_wrapper">
        <div class="dashboard dashboard_wrapper pr30 pr0-md">
        <%= render "shared/sidebar" %>
        <%= yield %>

        </div>
        </div>
      <% else %>
      <div class="row">
        <div class="col-lg-6 col-xl-5 offset-xl-1">
          <div class="main-title">
            <h2 class="mtitle">Become A Seller <br class="d-none d-md-block"> & Gain benefits</h2>
            <p>Sell Smarter, Grow Faster with Axis Marketplace Expand your business and reach
               millions of potential
              <br class="d-none d-xxl-block"> buyers on one of the fastest-growing e-commerce platforms.
               <br class="d-none d-xxl-block">
              <%= link_to new_account_path, class: "btn btn-thm signup-filter-btn" do %>
              Create Accounts
              <% end %>
               .</p>
          </div>
        </div>
        <div class="col-lg-6 col-xl-5 offset-xl-1">
          <div class="vendor_iconbox style2 d-block d-sm-flex">
            <span class="icon mt10 me-3"><img src="<%= asset_path "icons/step-1.svg" %>" ></span>
            <div class="details ms-0 ms-sm-4 mt-2 mt-sm-0">
              <h3 class="title">Step 1</h3>
              <p>Create an account on our website. It is fast and free.</p>
            </div>
          </div>
          <div class="vendor_iconbox style2 d-block d-sm-flex">
            <span class="icon mt10 me-3"><img src="<%= asset_path "icons/step-2.svg" %>" ></span>
            <div class="details ms-0 ms-sm-4 mt-2 mt-sm-0">
              <h3 class="title">Step 2</h3>
              <p>Upload your products.</p>
            </div>
          </div>
          <div class="vendor_iconbox style2 d-block d-sm-flex">
            <span class="icon mt10 me-3"><img src="<%= asset_path "icons/step-3.svg" %> " ></span>
            <div class="details ms-0 ms-sm-4 mt-2 mt-sm-0">
              <h3 class="title">Step 3</h3>
              <p>We’ll verify your account and then you can start <br class="d-none d-md-block"> selling!</p>
            </div>
          </div>
        </div>
      </div>


      <% end %>
    </div>

    <%= javascript_include_tag 'dashboard-script' %>

    <%= javascript_include_tag 'script' %>
    <script>

        document.addEventListener("DOMContentLoaded", function() {
          // Pie chart
          new Chart(document.getElementById("chartjs-dashboard-pie"), {
            type: "pie",
            data: {
              labels: ["Direct", "Affiliate", "E-mail", "Other"],
              datasets: [{
                data: [2602, 1253, 541, 1465],
                backgroundColor: [
                  window.theme.primary,
                  window.theme.warning,
                  window.theme.danger,
                  "#E8EAED"
                ],
                borderWidth: 5,
                borderColor: window.theme.white
              }]
            },
            options: {
              responsive: !window.MSInputMethodContext,
              maintainAspectRatio: false,
              cutoutPercentage: 70,
              legend: {
                display: false
              }
            }
          });
        });
    </script>

  </body>
</html>
